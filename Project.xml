<?xml version="1.0" encoding="utf-8"?>
<project>

    <app title="FNF: Psych Engine 0.4.2"
         file="PsychEngine042"
         packageName="com.shadowmario.psychengine042"
         package="com.shadowmario.psychengine042"
         main="Main"
         version="0.2.7"
         company="ShadowMario" />

    <!-- Switch export unique ID -->
    <set name="APP_ID" value="0x0100f6c013bbc000" />

    <app preloader="flixel.system.FlxPreloader" />

    <!-- Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2 -->
    <set name="SWF_VERSION" value="11.8" />

    <!-- ____________________________ Window Settings ___________________________ -->

    <!-- All targets -->
    <window width="1280" height="720" fps="60" background="#000000" hardware="true" vsync="false" />

    <!-- HTML5 -->
    <window if="html5" resizable="true" />

    <!-- Desktop -->
    <window if="desktop" orientation="landscape" fullscreen="false" resizable="true" vsync="false" />

    <!-- Mobile (Android + iOS): fullscreen, dynamic resolution -->
    <window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0" resizable="false" />

    <!-- Switch -->
    <window if="switch" orientation="landscape" fullscreen="true" width="0" height="0" resizable="true" />

    <!-- _____________________________ Path Settings ____________________________ -->

    <set name="BUILD_DIR" value="export/debug"   if="debug" />
    <set name="BUILD_DIR" value="export/release" unless="debug" />
    <set name="BUILD_DIR" value="export/32bit"   if="32" />

    <classpath name="source" />

    <!-- Audio: web uses mp3, everything else uses ogg -->
    <assets path="assets/preload" rename="assets" exclude="*.ogg" if="web" />
    <assets path="assets/preload" rename="assets" exclude="*.mp3" unless="web" />

    <!-- __________________________ Psych Engine Defines ________________________ -->

    <!-- Mods only on desktop (no file system access on mobile) -->
    <define name="MODS_ALLOWED" if="desktop" />

    <!-- Lua scripting only on Windows (linc_luajit Android support is unstable on 0.4.2) -->
    <define name="LUA_ALLOWED" if="windows" />

    <!-- Achievements on all platforms -->
    <define name="ACHIEVEMENTS_ALLOWED" />

    <!-- Videos: Windows and web only (no hxvlc in 0.4.2) -->
    <define name="VIDEOS_ALLOWED" if="web || windows" unless="32" />

    <!-- PRELOAD_ALL causes OOM on mobile — disabled on web and mobile -->
    <define name="PRELOAD_ALL"    unless="web || mobile" />
    <define name="NO_PRELOAD_ALL" unless="PRELOAD_ALL" />

    <!-- ___________________________ Library Sections __________________________ -->

    <library name="videos" preload="true" />

    <section if="PRELOAD_ALL">
        <library name="songs"  preload="true" />
        <library name="shared" preload="true" />
        <library name="week2"  preload="true" />
        <library name="week3"  preload="true" />
        <library name="week4"  preload="true" />
        <library name="week5"  preload="true" />
        <library name="week6"  preload="true" />
    </section>

    <section if="NO_PRELOAD_ALL">
        <library name="songs"  preload="false" />
        <library name="shared" preload="false" />
        <library name="week2"  preload="false" />
        <library name="week3"  preload="false" />
        <library name="week4"  preload="false" />
        <library name="week5"  preload="false" />
        <library name="week6"  preload="false" />
    </section>

    <!-- ______________________________ Assets _________________________________ -->

    <assets path="plugins/" rename='' if="VIDEOS_ALLOWED windows" />
    <assets path="dlls/"    rename='' if="VIDEOS_ALLOWED windows" />
    <assets path="assets/videos" library="videos" if="VIDEOS_ALLOWED" />

    <assets path="assets/songs"  library="songs"  exclude="*.ogg" if="web" />
    <assets path="assets/songs"  library="songs"  exclude="*.mp3" unless="web" />

    <assets path="assets/shared" library="shared" exclude="*.ogg" if="web" />
    <assets path="assets/shared" library="shared" exclude="*.mp3" unless="web" />

    <assets path="assets/week2"  library="week2"  exclude="*.ogg" if="web" />
    <assets path="assets/week2"  library="week2"  exclude="*.mp3" unless="web" />

    <assets path="assets/week3"  library="week3"  exclude="*.ogg" if="web" />
    <assets path="assets/week3"  library="week3"  exclude="*.mp3" unless="web" />

    <assets path="assets/week4"  library="week4"  exclude="*.ogg" if="web" />
    <assets path="assets/week4"  library="week4"  exclude="*.mp3" unless="web" />

    <assets path="assets/week5"  library="week5"  exclude="*.ogg" if="web" />
    <assets path="assets/week5"  library="week5"  exclude="*.mp3" unless="web" />

    <assets path="assets/week6"  library="week6"  exclude="*.ogg" if="web" />
    <assets path="assets/week6"  library="week6"  exclude="*.mp3" unless="web" />

    <assets path='example_mods'        rename='mods'             embed='false' if="MODS_ALLOWED" />
    <assets path='art/readme.txt'      rename='do NOT readme.txt' />
    <assets path="assets/fonts"        embed='true' />

    <!-- _______________________ Android Configuration _________________________ -->

    <section if="android">
        <!-- Target SDK 33 (Android 13), minimum SDK 21 (Android 5.0) -->
        <set name="ANDROID_TARGET_SDK_VERSION" value="33" />
        <set name="ANDROID_MIN_SDK_VERSION"    value="21" />

        <!-- Build architectures: arm64-v8a (modern devices) + armeabi-v7a (32-bit compat) -->
        <architecture name="arm64" />
        <architecture name="armv7" />

        <!-- Required Android permissions -->
        <android permission="android.permission.INTERNET" />
        <android permission="android.permission.VIBRATE" />
        <android permission="android.permission.WAKE_LOCK" />
        <android permission="android.permission.WRITE_EXTERNAL_STORAGE" />
        <android permission="android.permission.READ_EXTERNAL_STORAGE" />
    </section>

    <!-- _________________________ iOS Configuration ___________________________ -->

    <section if="ios">
        <!-- Minimum iOS 12.0 -->
        <set name="DEPLOYMENT_TARGET" value="12.0" />
    </section>

    <!-- _______________________________ Libraries ______________________________ -->

    <haxelib name="flixel" />
    <haxelib name="flixel-addons" />
    <haxelib name="hscript" />
    <haxelib name="flixel-ui" />
    <haxelib name="linc_luajit"  if="LUA_ALLOWED" />
    <haxelib name="faxe"         if="switch" />
    <!-- discord_rpc: desktop only — guarded in Discord.hx with #if desktop -->
    <haxelib name="discord_rpc"  if="desktop" />

    <!-- ______________________________ Haxedefines _____________________________ -->

    <!-- Disable focus-lost overlay (not useful on mobile) -->
    <haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />

    <!-- Strip debugger in release builds -->
    <haxedef name="FLX_NO_DEBUG" unless="debug" />

    <!-- Nape physics: release-mode optimisation -->
    <haxedef name="NAPE_RELEASE_BUILD" unless="debug" />

    <!-- _________________________________ Icons ________________________________ -->

    <icon path="art/icon16.png"  size='16' />
    <icon path="art/icon32.png"  size='32' />
    <icon path="art/icon64.png"  size='64' />
    <icon path="art/iconOG.png" />

</project>
